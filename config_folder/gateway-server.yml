server:
  port: 8501
spring:
  application:
    name: account-service
  zipkin:
    base-url: http://localhost:9411/
  data:
    cassandra:
      keyspace-name: panda_account_db
      port: 9042
      contact-points: localhost
      username: admin
      password: admin
      schema-action: create_if_not_exists
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8500/eureka


